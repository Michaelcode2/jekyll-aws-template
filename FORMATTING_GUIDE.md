# Інструкція з форматування та використання іконок

Цей документ пояснює, як можна стилізувати описи товарів та додавати іконки без зміни основного коду сайту.

## 1. Стандартне форматування (Markdown)

Продуктові описи підтримують стандартний синтаксис Markdown:

- **Жирний текст**: `**текст**`
- *Курсив*: `*текст*`
- Списки:
  ```markdown
  - Пункт 1
  - Пункт 2
  ```

## 2. Використання емодзі (Найпростіший спосіб)

Ви можете просто копіювати та вставляти емодзі безпосередньо в опис або в параметри `YAML` (наприклад, у список характеристик `features`).

**Приклад у फ्रंट-меттері (YAML):**
```yaml
features:
  - name: "🛡️ Безпека"
    value: "Шифрування даних"
  - name: "🚀 Швидкість"
    value: "Миттєва робота"
```

## 3. Використання системних іконок

На сайті є вбудована система іконок (SVG). Ви можете вставляти їх прямо в текст опису за допомогою Liquid-тегу.

**Синтаксис:**
```liquid
{% include icon.html name="check-circle" class="text-success" %} Ваш текст
```

### Доступні іконки
Список доступних назв (`name`) можна знайти у файлі `_data/icons.yml`. Основні:
- `check-circle` — галочка в колі
- `shield` — щит (безпека)
- `clock` — годинник (час/швидкість)
- `support` — гарнітура (підтримка)
- `integration` — стрілки (інтеграція)
- `certificate` — сертифікат
- `warranty` — гарантія
- `phone` / `mail` — телефон / пошта

### Як додати нову іконку
1. Відкрийте файл `_data/icons.yml`.
2. Додайте новий рядок: `назва: 'вміст SVG-тегу path'`.
3. Використовуйте її як `{% include icon.html name="назва" %}`.

## 4. Використання HTML-стилів

Оскільки Markdown дозволяє вставки HTML, ви можете додавати кольори та спеціальні блоки:

```html
<!-- Кольоровий текст -->
<span style="color: #2e7d32;">✔ Доступно на складі</span>

<!-- Інформаційний блок -->
<div style="background: #e3f2fd; padding: 15px; border-radius: 8px; border-left: 5px solid #2196f3; margin: 10px 0;">
  <strong>Важливо:</strong> Оновлення відбуваються автоматично.
</div>
```

---
*Примітка: При редагуванні файлів у `_products/`, будьте уважні з відступами у полі `description`.*
